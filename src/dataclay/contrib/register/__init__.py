"""Contrib classes that should be registered into dataClay.

The classes available here should be used through their sources and registered
into dataClay.

No application should never import dataclay.contrib.register module directly
(nor any submodule).
"""
